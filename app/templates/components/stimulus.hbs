<span class="mr-3 stimulus {{if voltage "active"}}">
  <FaIcon @icon="head-side-brain" @prefix="fal" />
  <span>
    {{#if voltage}}
      {{t "states.stimulation-on"}}
    {{else}}
      {{t "states.stimulation-off"}}
    {{/if}}
  </span>
</span>
