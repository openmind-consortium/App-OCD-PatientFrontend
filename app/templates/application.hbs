<body class="app">

  <div class={{if settings.isLight "bg-light-mode" "bg-dark-mode"}} >

    <div class="info-bar">
      <div class="info-clock mr-auto">
        <Clock @showClock={{true}} />
      </div>

      <div class="info-device d-flex">
        <Stimulus @voltage={{model.stimulation_voltage}}/>
        <Rec @recording={{model.recording}} />
        <Battery @value={{model.battery}} />
      </div>

    </div>

    <div class="row">
      <div class="col-md-3">
        <Menu />
      </div>
      <div class="col-md-9">
        {{outlet}}
      </div>
    </div>
  </div>
</body>
